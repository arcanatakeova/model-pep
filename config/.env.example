# ARCANA AI — Environment Variables
# Copy to .env and fill in your keys. NEVER commit .env to git.
# REQUIRED = Must have before Phase 1. OPTIONAL = Can add later.

# === LLM (REQUIRED — Phase 1) ===
OPENROUTER_API_KEY=sk-or-v1-your-key-here
OPENAI_API_KEY=sk-your-key-here
# OpenAI key is for embeddings only (text-embedding-ada-002). OpenRouter doesn't proxy embeddings.

# === Database (REQUIRED — Phase 1) ===
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key
POSTGRES_PASSWORD=arcana_local
# POSTGRES_PASSWORD only used for local Docker DB. Supabase cloud handles its own auth.

# === Social (REQUIRED — Phase 2) ===
X_API_KEY=your-x-api-key
X_API_SECRET=your-x-api-secret
X_ACCESS_TOKEN=your-x-access-token
X_ACCESS_SECRET=your-x-access-token-secret
# Basic tier: $200/month. 15K reads, 50K writes per month.

# === Notifications (REQUIRED — Phase 1) ===
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-chat-id

# === Market Data (REQUIRED — Phase 3) ===
BIRDEYE_API_KEY=your-birdeye-key
FINNHUB_API_KEY=your-finnhub-key
# DexScreener: No key needed (free public API)
# Rugcheck: No key needed (free public API)

# === Trading — Solana (REQUIRED — Phase 3) ===
SOLANA_PRIVATE_KEY=your-burner-wallet-base58-private-key
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-helius-key
# CRITICAL: Use a DEDICATED BURNER WALLET. Fund with ONLY trading capital.

# === Trading — Polymarket (REQUIRED — Phase 3) ===
POLYMARKET_PRIVATE_KEY=your-polygon-wallet-private-key
# Polymarket operates on Polygon. Separate wallet from Solana.

# === OPTIONAL — Add when needed ===
UNUSUAL_WHALES_API_KEY=your-uw-key
# $250/month. Can skip initially, add when trading is profitable.

COINBASE_API_KEY=your-coinbase-key
COINBASE_API_SECRET=your-coinbase-secret
# Only needed for CEX trading (BTC/ETH). Can skip initially.

HEYGEN_API_KEY=your-heygen-key
# $5 PAYG to start. Needed for UGC video production (Phase 4).

MAKEUGC_API_KEY=your-makeugc-key
# Enterprise tier required for API. Contact sales. Skip initially, use HeyGen.

ELEVENLABS_API_KEY=your-elevenlabs-key
# $5/mo starter. Needed for podcast factory (Phase 5).

STRIPE_SECRET_KEY=sk_live_your-stripe-key
GUMROAD_ACCESS_TOKEN=your-gumroad-token
# Needed when selling digital products (Phase 4).

KEEPA_API_KEY=your-keepa-key
# EUR 19/mo. For price monitoring deal alerts (Phase 5).

DEEPL_API_KEY=your-deepl-key
# Free tier: 500K chars/mo. For translation services (Phase 5).

BEEHIIV_API_KEY=your-beehiiv-key
# For newsletter (Phase 5).

# === Agent Config ===
DRY_RUN=true
# Set to false when ready for live trading. START WITH TRUE. Run for 2 weeks minimum.
TRADING_CAPITAL=1000
MAX_POSITION_PCT=5
STOP_LOSS_PCT=15
DAILY_DRAWDOWN_LIMIT_PCT=10
ORCHESTRATOR_INTERVAL_MINUTES=15
LOG_LEVEL=INFO
