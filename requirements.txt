# AI Trader — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────
requests>=2.31.0          # HTTP client for all API calls
pandas>=2.0.0             # Data manipulation and time series analysis
numpy>=1.24.0             # Numerical computations for indicators

# ── Optional (enhanced features) ─────────────────────────────────────────────
yfinance>=0.2.18          # Yahoo Finance: stock & ETF data (unofficial API)
python-dotenv>=1.0.0      # Load API keys from .env file

# ── Live Trading (optional, only needed for live mode) ────────────────────────
# ccxt>=4.0.0             # Unified crypto exchange API (Binance, Coinbase, etc.)
#                         # Uncomment and install for live trading

# ── Development / Testing ────────────────────────────────────────────────────
# pytest>=7.0.0
# pytest-mock>=3.0.0
