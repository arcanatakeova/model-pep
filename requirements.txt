# ARCANA AI — Dependencies
# Python 3.11+

# ── Core ─────────────────────────────────────────────────────────────────────
httpx>=0.27                 # Async HTTP client for all API calls
pydantic>=2.0               # Data validation and settings
python-dotenv>=1.0          # Load API keys from .env

# ── LLM & Agent Framework ───────────────────────────────────────────────────
langchain>=0.3              # LLM framework
langgraph>=0.2              # Agent graph orchestration
openai>=1.0                 # OpenAI SDK (used via OpenRouter)

# ── Database ─────────────────────────────────────────────────────────────────
supabase>=2.0               # Supabase client (PostgreSQL + pgvector)

# ── Scheduling ───────────────────────────────────────────────────────────────
apscheduler>=3.10           # Job scheduling for periodic tasks

# ── Trading — Solana ─────────────────────────────────────────────────────────
solders>=0.21               # Rust-based Solana types (Keypair, Pubkey)
solana>=0.34                # Solana Python SDK (RPC client)
base58>=2.1                 # Base58 encoding for Solana keys

# ── Trading — Polymarket ─────────────────────────────────────────────────────
# py-clob-client>=0.17     # Polymarket CLOB client (Linux x86_64 only)

# ── Social Media ─────────────────────────────────────────────────────────────
tweepy>=4.14                # X/Twitter API client

# ── Payments ─────────────────────────────────────────────────────────────────
stripe>=8.0                 # Stripe payment processing

# ── Notifications ────────────────────────────────────────────────────────────
discord-webhook>=1.3        # Discord webhook notifications
python-telegram-bot>=21.0   # Telegram Bot API

# ── Legacy (existing trader) ─────────────────────────────────────────────────
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
